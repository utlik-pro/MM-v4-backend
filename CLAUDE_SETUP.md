# Настройка Claude CLI

## 🚀 Быстрый старт

### 1. Получение API ключа
1. Перейдите на [console.anthropic.com](https://console.anthropic.com)
2. Войдите в свой аккаунт
3. Перейдите в раздел "API Keys"
4. Создайте новый API ключ

### 2. Настройка API ключа

**Временная настройка (только для текущей сессии):**
```bash
export ANTHROPIC_API_KEY="sk-ant-api03-ваш-ключ-здесь"
```

**Постоянная настройка:**
1. Откройте файл `~/.zshrc` в редакторе:
   ```bash
   nano ~/.zshrc
   ```
2. Найдите строку с `export ANTHROPIC_API_KEY="your-api-key-here"`
3. Замените `your-api-key-here` на ваш реальный API ключ
4. Сохраните файл и перезагрузите терминал:
   ```bash
   source ~/.zshrc
   ```

### 3. Проверка работы
```bash
claude "Привет! Как дела?"
```

## 📖 Использование

### Основные команды:
```bash
# Простой запрос
claude "Ваш вопрос здесь"

# Интерактивный режим
claude -i

# Чтение из файла
claude -f filename.txt

# Выбор модели
claude -m claude-3-5-sonnet-20241022 "Запрос"
```

### Доступные модели:
- `claude-3-5-sonnet-20241022` (по умолчанию)
- `claude-3-5-haiku-20241022`
- `claude-3-opus-20240229`

## 🔧 Устранение неполадок

### Ошибка "Не найден API ключ"
- Убедитесь, что API ключ правильно установлен
- Проверьте, что ключ начинается с `sk-ant-api03-`
- Перезагрузите терминал: `source ~/.zshrc`

### Ошибка "Invalid API key"
- Проверьте правильность API ключа
- Убедитесь, что у вас есть активная подписка на Anthropic

## 💡 Примеры использования

```bash
# Анализ кода
claude -f script.py "Найди ошибки в этом коде"

# Перевод текста
claude "Переведи на английский: Привет, как дела?"

# Объяснение концепции
claude "Объясни квантовую физику простыми словами"

# Работа с проектом
claude -f README.md "Проанализируй этот проект и дай рекомендации"
``` 