# 🚀 Деплой на Render.com

Инструкция по развертыванию автоматической синхронизации bir.by → ElevenLabs Knowledge Base на платформе Render.com.

## 📋 Что деплоим

**Автоматический пайплайн:**
```
bir.by API → обработка данных → ElevenLabs Knowledge Base → ElevenLabs Agent
```

**Результат:**
- ✅ 18 кварталов обновляются автоматически каждый час
- ✅ 810+ квартир синхронизируются с правильной UTF-8 кодировкой
- ✅ Нулевое обслуживание, работает в фоне
- ✅ Бесплатно ($0/месяц)

---

## 🎯 Предварительные требования

### 1. GitHub аккаунт
- Проект должен быть в GitHub репозитории
- Убедитесь что `.env` в `.gitignore` (не коммитим секреты!)

### 2. Render.com аккаунт
- Зарегистрируйтесь на [render.com](https://render.com)
- Бесплатно, не требует кредитной карты

### 3. ElevenLabs API ключи
- API Key: получите в [ElevenLabs Settings → API Keys](https://elevenlabs.io/app/settings/api-keys)
- Agent ID: найдите в URL агента `https://elevenlabs.io/app/conversational-ai/agents/{AGENT_ID}/edit`

---

## 📦 Шаг 1: Подготовка репозитория

### 1.1 Проверьте файлы

Убедитесь что в проекте есть:

```bash
MM-RAG/
├── render.yaml                    # ✅ Конфигурация Render
├── sync-with-monitoring.py        # ✅ Основной скрипт мониторинга
├── elevenlabs_auto_sync.py        # ✅ Скрипт синхронизации ElevenLabs
├── requirements.txt               # ✅ Зависимости Python
├── .env.example                   # ✅ Шаблон переменных окружения
├── .gitignore                     # ✅ Исключает .env из git
└── quarters/                      # Создастся автоматически
```

### 1.2 Проверьте .gitignore

Убедитесь что `.env` НЕ попадет в git:

```bash
git status
# .env не должен быть в списке изменений!
```

### 1.3 Закоммитьте изменения

```bash
git add render.yaml .env.example .gitignore
git commit -m "Add Render.com deployment configuration"
git push origin main
```

---

## 🌐 Шаг 2: Создание сервиса на Render

### 2.1 Подключите GitHub

1. Войдите в [Render Dashboard](https://dashboard.render.com/)
2. Нажмите **"New +"** → **"Cron Job"**
3. Выберите **"Connect a repository"**
4. Авторизуйте Render для доступа к вашему GitHub
5. Выберите репозиторий `MM-v4-backend`

### 2.2 Render автоматически найдет render.yaml

Render обнаружит файл `render.yaml` и предложит использовать его конфигурацию.

**Проверьте настройки:**
- **Name**: `mm-rag-elevenlabs-sync`
- **Environment**: `Python 3`
- **Plan**: `Free`
- **Schedule**: `0 * * * *` (каждый час)
- **Build Command**: `pip install -r requirements.txt`
- **Start Command**: `python3 sync-with-monitoring.py --check --upload-to-elevenlabs`

---

## 🔐 Шаг 3: Добавление переменных окружения

### 3.1 В Render Dashboard

1. Откройте созданный Cron Job
2. Перейдите во вкладку **"Environment"**
3. Добавьте переменные:

| Key | Value | Пример |
|-----|-------|--------|
| `ELEVENLABS_API_KEY` | Ваш API ключ | `sk_16f32...` |
| `ELEVENLABS_AGENT_ID` | ID вашего агента | `agent_8901...` |

**⚠️ ВАЖНО:** Не коммитьте эти значения в git!

### 3.2 Сохраните изменения

Нажмите **"Save Changes"**

---

## 🎬 Шаг 4: Первый запуск

### 4.1 Ручной запуск (для теста)

1. В Render Dashboard откройте ваш Cron Job
2. Нажмите **"Manual Deploy"** → **"Clear build cache & deploy"**
3. Дождитесь завершения сборки (~2-3 минуты)

### 4.2 Проверьте логи

Во вкладке **"Logs"** вы должны увидеть:

```
============================================================
🕐 Проверка изменений: 2025-11-24 18:00:01
============================================================
📥 Получение данных с https://bir.by/ai/json_ai.php...
🔄 Обнаружены изменения!
💾 База знаний обновлена: quarters/knowledge-base.json

📂 Сохранение данных по кварталам:
  ✏️  Квартал 21-Западный: обновлен (152 квартир)
  ✏️  Квартал 27-Happy-Planet: обновлен (159 квартир)
  ...

☁️  Синхронизация с ElevenLabs...
   📝 Изменено файлов: 18
✅ Синхронизация с ElevenLabs завершена успешно
```

### 4.3 Проверьте ElevenLabs

1. Откройте [ElevenLabs Knowledge Base](https://elevenlabs.io/app/conversational-ai/knowledge-base)
2. Убедитесь что появились 18 документов с кварталами
3. Откройте [настройки агента](https://elevenlabs.io/app/conversational-ai/agents)
4. Проверьте что документы привязаны к агенту

---

## ⏰ Шаг 5: Автоматический запуск

### 5.1 Расписание

По умолчанию cron запускается **каждый час** (0-я минута каждого часа):
```
0 * * * *
```

### 5.2 Изменение расписания

Если хотите изменить частоту, отредактируйте `render.yaml`:

**Каждые 30 минут:**
```yaml
schedule: "*/30 * * * *"
```

**Каждые 2 часа:**
```yaml
schedule: "0 */2 * * *"
```

**Каждый день в 9:00:**
```yaml
schedule: "0 9 * * *"
```

После изменения:
```bash
git add render.yaml
git commit -m "Update cron schedule"
git push origin main
```

Render автоматически применит новое расписание.

---

## 📊 Мониторинг и отладка

### Просмотр логов

**В Render Dashboard:**
1. Откройте ваш Cron Job
2. Вкладка **"Logs"**
3. Выберите нужный запуск из истории

**Фильтрация:**
- Поиск по ключевым словам: `✅`, `❌`, `ERROR`
- Время выполнения отображается для каждого запуска

### История запусков

Render хранит историю всех запусков cron job:
- ✅ Успешные (зеленая галочка)
- ❌ Ошибки (красный крестик)
- ⏱️ Время выполнения

### Типичные проблемы

**Проблема:** `ModuleNotFoundError: No module named 'requests'`
- **Решение:** Проверьте что `requirements.txt` содержит `requests>=2.31.0`

**Проблема:** `KeyError: 'ELEVENLABS_API_KEY'`
- **Решение:** Добавьте переменные окружения в Render Dashboard → Environment

**Проблема:** `TimeoutError` при загрузке в ElevenLabs
- **Решение:** Это известная проблема ElevenLabs API, скрипт автоматически повторит попытку

**Проблема:** Cron не запускается
- **Решение:** Проверьте синтаксис расписания в `render.yaml`

---

## 🗂️ Персистентное хранилище (опционально)

### Зачем нужно

По умолчанию Render создает новое окружение при каждом запуске. Это означает:
- `quarters/` директория создается заново
- История версий (`version-history.json`) сбрасывается
- Но это **не проблема**, т.к. скрипт работает с полными данными

### Если хотите сохранять историю

В `render.yaml` уже настроен persistent disk:

```yaml
disk:
  name: quarters-data
  mountPath: /opt/render/project/src/quarters
  sizeGB: 1
```

Это сохранит:
- `quarters/.quarter_hashes.json` - кеш хешей
- `quarters/version-history.json` - история версий
- Логи между запусками

---

## 💰 Стоимость

| Компонент | План | Стоимость |
|-----------|------|-----------|
| Cron Job | Free | $0/мес |
| Persistent Disk (опц.) | 1 GB | $0.25/мес |
| **ИТОГО** | | **$0-0.25/мес** |

**Free tier включает:**
- Неограниченные cron jobs
- 100 build minutes/месяц (более чем достаточно)
- Автоматические деплои из GitHub

---

## 🔄 Обновление кода

### Автоматические деплои

Render автоматически разворачивает изменения при push в GitHub:

```bash
# Внесли изменения в код
git add .
git commit -m "Улучшения в логике синхронизации"
git push origin main

# Render автоматически:
# 1. Обнаружит изменения
# 2. Пересоберет образ
# 3. Применит изменения к cron job
```

### Ручной деплой

Если нужно форсировать обновление:
1. Render Dashboard → ваш Cron Job
2. **"Manual Deploy"** → **"Deploy latest commit"**

---

## 🧪 Тестирование перед деплоем

### Локальный тест

Перед деплоем протестируйте локально:

```bash
# 1. Установите зависимости
pip install -r requirements.txt

# 2. Создайте .env с реальными ключами
cp .env.example .env
# Отредактируйте .env, добавьте ваши ключи

# 3. Запустите синхронизацию
python3 sync-with-monitoring.py --check --upload-to-elevenlabs

# Проверьте что:
# ✅ Данные загружаются с bir.by
# ✅ Генерируются quarters/*.md
# ✅ Загружается в ElevenLabs KB
# ✅ Агент обновляется
```

---

## 📝 Checklist деплоя

- [ ] Проект в GitHub
- [ ] `.env` в `.gitignore`
- [ ] `render.yaml` создан и закоммичен
- [ ] Аккаунт на Render.com создан
- [ ] Репозиторий подключен к Render
- [ ] Переменные окружения добавлены в Render Dashboard
- [ ] Первый manual deploy успешен
- [ ] Логи показывают успешную синхронизацию
- [ ] ElevenLabs KB содержит 18 документов
- [ ] Агент обновлен и видит документы
- [ ] Cron schedule настроен правильно

---

## 🎉 Готово!

Поздравляю! Теперь ваш пайплайн работает автоматически:

```
bir.by → (каждый час) → Render Cron Job → ElevenLabs KB → Agent
```

**Что происходит автоматически:**
1. Каждый час Render запускает cron job
2. Скачиваются свежие данные с bir.by
3. Обрабатываются 18 кварталов
4. Генерируются markdown файлы с правильной UTF-8 кодировкой
5. Загружаются в ElevenLabs Knowledge Base
6. Агент автоматически получает обновленную информацию

**Никакого обслуживания не требуется!** 🚀

---

## 📞 Поддержка

**Render.com:**
- [Документация Cron Jobs](https://render.com/docs/cronjobs)
- [Community Forum](https://community.render.com/)

**ElevenLabs:**
- [API Documentation](https://elevenlabs.io/docs/api-reference)
- [Knowledge Base Guide](https://elevenlabs.io/docs/conversational-ai/knowledge-base)

**Проект:**
- GitHub Issues: [utlik-pro/MM-v4-backend/issues](https://github.com/utlik-pro/MM-v4-backend/issues)
