# 🔄 Настройка автоматического обновления MM-RAG

## Обзор

Система MM-RAG поддерживает несколько способов автоматического обновления данных. По умолчанию интервал установлен на **60 минут (1 час)**.

## Способы запуска

### Способ 1: Простой запуск в терминале

Для быстрого запуска мониторинга с почасовым обновлением:

```bash
cd /Users/admin/MM-RAG
./hourly-update.sh
```

Скрипт будет работать в фоне и обновлять данные каждый час.

### Способ 2: Использование cron (рекомендуется для Linux/Mac)

#### Установка cron задачи:

1. Откройте редактор crontab:
```bash
crontab -e
```

2. Добавьте одну из следующих строк:

```bash
# Обновление каждый час в 0 минут
0 * * * * /Users/admin/MM-RAG/cron-update.sh

# Обновление каждый час только в рабочее время (9:00-18:00)
0 9-18 * * * /Users/admin/MM-RAG/cron-update.sh

# Обновление каждые 2 часа
0 */2 * * * /Users/admin/MM-RAG/cron-update.sh
```

3. Сохраните и выйдите из редактора (`:wq` для vim)

#### Проверка установленных задач:
```bash
crontab -l
```

#### Удаление всех cron задач:
```bash
crontab -r
```

### Способ 3: LaunchAgent для macOS

#### Установка LaunchAgent:

1. Скопируйте plist файл в LaunchAgents:
```bash
cp /Users/admin/MM-RAG/com.mmrag.hourly-update.plist ~/Library/LaunchAgents/
```

2. Загрузите службу:
```bash
launchctl load ~/Library/LaunchAgents/com.mmrag.hourly-update.plist
```

#### Управление службой:

```bash
# Остановить службу
launchctl unload ~/Library/LaunchAgents/com.mmrag.hourly-update.plist

# Перезапустить службу
launchctl unload ~/Library/LaunchAgents/com.mmrag.hourly-update.plist
launchctl load ~/Library/LaunchAgents/com.mmrag.hourly-update.plist

# Проверить статус
launchctl list | grep mmrag
```

### Способ 4: Запуск с кастомным интервалом

Вы можете запустить мониторинг с любым интервалом (в минутах):

```bash
# Обновление каждые 30 минут
python3 sync-with-monitoring.py --monitor --interval 30

# Обновление каждые 2 часа (120 минут)
python3 sync-with-monitoring.py --monitor --interval 120

# Обновление каждые 6 часов (360 минут)
python3 sync-with-monitoring.py --monitor --interval 360
```

## Логирование

### Просмотр логов мониторинга:

```bash
# Основной лог мониторинга
tail -f /Users/admin/MM-RAG/monitoring_log.txt

# Лог cron задач
tail -f /Users/admin/MM-RAG/cron-update.log

# Лог LaunchAgent (macOS)
tail -f /Users/admin/MM-RAG/launchd-stdout.log
tail -f /Users/admin/MM-RAG/launchd-stderr.log
```

### Очистка логов:

```bash
# Очистить лог мониторинга
> /Users/admin/MM-RAG/monitoring_log.txt

# Очистить лог cron
> /Users/admin/MM-RAG/cron-update.log
```

## Проверка работы

### Проверить последнее обновление:

```bash
# Посмотреть время последнего обновления
grep "Проверка обновлений" monitoring_log.txt | tail -1

# Посмотреть последние изменения
grep "Изменения обнаружены" monitoring_log.txt | tail -5
```

### Выполнить одноразовую проверку:

```bash
python3 sync-with-monitoring.py --check
```

## Остановка автообновления

### Для процесса в терминале:
Нажмите `Ctrl+C`

### Для cron:
```bash
# Удалить задачу из crontab
crontab -e
# Удалите соответствующую строку и сохраните
```

### Для LaunchAgent (macOS):
```bash
launchctl unload ~/Library/LaunchAgents/com.mmrag.hourly-update.plist
rm ~/Library/LaunchAgents/com.mmrag.hourly-update.plist
```

## Рекомендации

1. **Для production**: Используйте cron или LaunchAgent для надежной работы
2. **Для тестирования**: Используйте простой запуск в терминале
3. **Интервал обновления**: 
   - 60 минут - оптимально для большинства случаев
   - 30 минут - для активного мониторинга
   - 120+ минут - для экономии ресурсов

## Troubleshooting

### Проблема: Скрипт не запускается через cron

Решение:
```bash
# Проверьте права на выполнение
chmod +x /Users/admin/MM-RAG/cron-update.sh
chmod +x /Users/admin/MM-RAG/hourly-update.sh

# Проверьте путь к python3
which python3
# Обновите путь в скриптах если необходимо
```

### Проблема: Нет обновлений в логах

Решение:
```bash
# Проверьте, работает ли процесс
ps aux | grep sync-with-monitoring

# Проверьте доступность источника данных
curl -I https://bir.by/ai/json_ai.php
```

### Проблема: Слишком большие логи

Решение:
```bash
# Настройте ротацию логов или очищайте их периодически
echo "0 0 * * 0 > /Users/admin/MM-RAG/monitoring_log.txt" | crontab -
```

## Контакты

При возникновении проблем проверьте:
- Файл логов: `monitoring_log.txt`
- Статус последнего обновления: `version-history.json`
- Количество объектов: `quarters/by-quarters/*.json`

---

*Документация обновлена: 2025-09-22*